name: Release Windows EXE

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (example: v1.0.0)"
        required: true
      prerelease:
        description: "Mark this release as a prerelease"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller PyQt5 python-dateutil

      - name: Build EXE
        run: pyinstaller --onefile --noconsole app.py

      - name: Prepare release asset
        id: package
        shell: pwsh
        run: |
          $assetName = "todoprogram-${{ inputs.tag }}.exe"
          Copy-Item -LiteralPath "dist\app.exe" -Destination "dist\$assetName" -Force
          "asset_name=$assetName" >> $env:GITHUB_OUTPUT

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: dist\${{ steps.package.outputs.asset_name }}

      - name: Create GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $args = @(
            'release', 'create', '${{ inputs.tag }}',
            "dist\${{ steps.package.outputs.asset_name }}",
            '--title', '${{ inputs.tag }}',
            '--generate-notes',
            '--target', '${{ github.sha }}'
          )

          if ('${{ inputs.prerelease }}' -eq 'true') {
            $args += '--prerelease'
          }

          & gh @args
